Hi — status on the three items, plus one blocker worth flagging.

**Git push**: done. Pushed to refine_illumination (e3ecb5e..571efe6) — mpi_object_halo.py, validate_object_halo.py, the wired mpi_AP_step/mpi_AP_step_timed, the sbatch drivers, and the report are all in now. You should be able to reproduce the ladder from the repo.

**Item 1 (memory/pixel work), split in two:**
- Safe half — done. Hoisted the eps reduction out of the per-iteration loop (it only depends on normalization, which is static, so no reason to force a device sync computing it every call). Also restricted the final `img_global / (normalization + eps)` division to just `tiles_own` when object_halo is set, instead of the whole (Nx,Ny) canvas — the rest was never read by anything downstream.
- The bigger half (allocating img0 at bbox size) — held off. Turns out that's a real coordinate rebasing job, not a quick patch: overlap_cuda indexes with global canvas coordinates and periodic %-wraparound, so shrinking the buffer means split_cuda on the *next* iteration needs the same rebased, bbox-sized buffer too — touching PASS A, not just PASS B. That's architecturally the same lift as the tiled-object stash from July 21 (mpi_AP_step_tiled/_get_tile_info). Didn't want to fold a change that size into this pass without flagging it first — happy to pick it up as its own piece if you want it now rather than later.

One correction on your line numbers for this item: I couldn't find the full-canvas allocation/max-reduction at :426/:435 in the current mpi_AP_step — those lines are docstring text and a split_cuda call. The actual img0 allocation and eps reduction are elsewhere in the function (I checked the memory-growth math itself and it's right — 28.8→217.8 MB, 7.56x, matches your 29.1→218.7/7.5x almost exactly — just the line citation was off, maybe an older copy). Also checked the small thing you flagged, validate_object_halo.py:100 `contains = True` — that variable doesn't exist anywhere in the file, and since it was never pushed before now you can't have been reading it from the repo. Guessing a stale draft got looked at somewhere. Not chasing a fix for something I can't find.

**Item 2 (setup_halo rebuilding every sync)**: implemented. Split mpi_halo.py's setup_halo into setup_halo_geometry() (the positions-only part — 3 Allgathervs, the global Gramiam_plan, XOR-filtering boundary pairs) and exchange_halo_frames() (the content-dependent part — exchanging current frame data, rebuilding H_cross). mpi_gramian_sync and mpi_AP_step now take an optional halo_geometry, built once and reused across every sync call while positions are frozen; None falls back to the old always-rebuild behavior so nothing existing breaks.

One honest caveat: I couldn't verify this on real hardware yet. validate-sync hits a separate, pre-existing environment problem — Gramiam_calc_cuda's zQQz kernel uses jitify=True for runtime compilation, which needs actual CUDA toolkit headers that don't exist anywhere in this pip-only cupy environment (same category of gap as the libcublas.so.12 issue from the object-halo work, just the headers side instead of the runtime-library side). This is upstream of anything I changed today — it fails before setup_halo_geometry is ever reached, and would've hit the original unmodified code the same way, since this is the first time all session anything has actually exercised the Gramian sync path (every gate so far only touched exchange_object_halo, which doesn't use this kernel). Tried the obvious cheap fix (pip install nvidia-cuda-nvcc-cu12, 40MB) — didn't resolve it, and jitify isn't surfacing which specific header/symbol is missing, just a generic "Runtime compilation failed." So: item 2 is implemented and I'm confident in the logic (it's a straightforward extraction of the existing steps, nothing new invented), but not yet confirmed correct on GPU. Will keep chasing the environment issue unless you'd rather I not sink more time into it.

**Item 3 (coarse gauge on a cadence)**: implemented as a new function, mpi_AP_step_cadence, rather than modifying mpi_AP_step directly — didn't want to risk regressing what's already gate-verified for a genuinely new algorithmic mode. Every object_sync_interval-th iteration it does the real thing (PASS B + exchange_object_halo, same as today). On the iterations in between: no cross-rank pixel communication at all — each rank divides by its own local coverage and applies mpi_coarse_gauge_sync (one complex number per rank via Allgather) to keep tiles in global phase agreement. Wrote a dedicated validator, validate_cadence_sync, that checks final NMSE against the same two baselines you cited (coarse-gauge-only ~0.02-0.03, no-sync ~0.42-0.53) rather than just timing it. Haven't run it yet — it doesn't touch Gramiam_calc_cuda/jitify at all (mpi_coarse_gauge_sync is pure Allgather + numpy linear algebra), so it isn't blocked by the item-2 issue above. Next thing I'll actually run.

None of this is pushed yet — wanted to get your eyes on it first given item 2 is unverified and item 3 is a new algorithmic mode, not just a perf change. Let me know if you want it up regardless with the caveats noted, or held until item 2's blocker clears and item 3 has real NMSE numbers.
